import React from 'react'

function page() {
  return (
    <div>
    //await new Promise((resolve) => setTimeout (resolve,5000); 
        simple page. make fetch call of any api either for server or for client
    </div>
  )
}

export default page
