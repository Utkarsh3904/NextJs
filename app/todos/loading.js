import React from 'react'

function loading() {
  return (
    <div>
      loading ...
      this will show up when its neighbour page shows any delay its this code is enough
    </div>
  )
}

export default loading
